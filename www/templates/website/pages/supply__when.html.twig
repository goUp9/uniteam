{% extends "base.html.twig" %}
{% block content %}
    <div id="wrapper">    
    {% include 'commons/header.html.twig'%}
    <main>
        <div id="query-type">
            <img src="{{img.uin_supply.src}}" alt="{{img.uin_supply.alt}}" /><h1>supplying</h1>
        </div>
        <div class="steps-heading">
            <div clickable><a href="{{link.home.href}}"><img class="grayscale opacity" src="{{img.step_what.src}}" alt="{{img.step_what.alt}}"/></a><p class="grayscale">&RightArrow;</p></div>
            <div><a href="{{link.supply__where.href}}"><img class="grayscale opacity" src="{{img.step_where.src}}" alt="{{img.step_where.alt}}"/></a><p class="grayscale">&RightArrow;</p></div>
            <div><img src="{{img.step_when.src}}" alt="{{img.step_when.alt}}"/></div>
        </div>
        <div id="form" data-ng-controller="SupplySearchCtrl as when">
            <h1>Please set up your opening hours:</h1>
            <div class="set-ups">
                <p ng-click="when.quickSelectFlag=!when.quickSelectFlag" class="hint" id="quick-select">Quick-selection days</p>
                <ul ng-show="when.quickSelectFlag">
                    <li data-ng-click="when.set_alldays()">24/7</li>
                    <li data-ng-click="when.weekends()">Weekends</li>
                    <li data-ng-click="when.weekdays()">Weekdays</li>
                </ul>
            </div>
            <ul>                
                <li data-ng-repeat="day in weekDays"><p>{{'{{day.title}}'}}:</p>
                    <input type="checkbox" data-ng-click="when.setClosedFlag($index)" data-ng-model="when.closedFlags[$index]" data-ng-init="when.closedFlags[$index]=false"/><label class="closed">Closed</label><br/>
                    <label>from </label>
                    <hour-select selected="when.formData[$index].from.hours"></hour-select>
                    <minutes-select selected="when.formData[$index].from.minutes"></minutes-select>
                    <!--<input class="time" type="number" id="input-from-hours-{{'{{$index}}'}}" data-ng-change="when.validate_from_hours($index)" min="0" max="23" data-ng-model="when.formData[$index].from.hours" data-ng-init="when.formData[$index].from.hours=0"/>-->
                    <!--<input class="time" type="number" id="input-from-minutes-{{'{{$index}}'}}" data-ng-change="when.validate_from_mins($index)" min="0" max="59" data-ng-model="when.formData[$index].from.mins" data-ng-init="when.formData[$index].from.mins=0"/>-->
                    <label>to </label>
                    <hour-select selected="when.formData[$index].to.hours"></hour-select>
                    <minutes-select selected="when.formData[$index].to.minutes"></minutes-select>
                    <!--<input class="time" type="number" id="input-to-hours-{{'{{$index}}'}}" data-ng-change="when.validate_to_hours($index)" min="0" max="23" data-ng-model="when.formData[$index].to.hours" data-ng-init="when.formData[$index].to.hours=23"/>
                    <input class="time" type="number" id="input-to-minutes-{{'{{$index}}'}}" data-ng-change="when.validate_to_mins($index)" min="0" max="59" data-ng-model="when.formData[$index].to.mins" data-ng-init="when.formData[$index].to.mins=59"/>-->
          
                </li>
            </ul>
            <div class="btn" id="btn__save" data-ng-click="when.submit()">
                <p>Continue</p>
            </div>
        </div>
    </main>
    <div id="push-footer"></div>                
    </div>
    {% include 'commons/footer.html.twig'%}
    
{% endblock %}